@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@{
    Layout = "_Layout";
}
@model List<Books.Models.Book>




@if(Model.Count > 0)
{
    @foreach (var book in Model)
    {
        <partial name="_ResultLine" model="book" />
    }
}
else
{
    <partial name="_ErrorMessage" />
}
