@*--- addTagHelper := Utiliser la syntaxe RAZER ---*@
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

@*--- Liaison a _Layout ---*@
@{
    Layout = "_Layout";
}

@*--- Liaison au modele de donnee ---*@
@*@model List<Recherche_livres.Models.PageInfo>*@
@model Recherche_livres.Models.PageInfo
@{
    var pageInfo = Model;
    <partial name="_Title" model="pageInfo" />
}


@*@foreach (var pageInfo in Model)
{
    <partial name="_Title" model="pageInfo" />
}*@









